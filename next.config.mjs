
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your existing configuration can go here.
  // By not including "output: 'export'", we default to server-side rendering.
};

export default nextConfig;
